PyQt6==6.9.1

# Распознавание речи для вкладки «Мат». Работает в отдельном процессе: Qt6 и
# ctranslate2 конфликтуют при загрузке нативных библиотек и в одном процессе
# роняют программу. Модель в поставку не входит и скачивается при первом
# использовании — она весит несколько гигабайт, а нужна не всем.
faster-whisper==1.2.1
